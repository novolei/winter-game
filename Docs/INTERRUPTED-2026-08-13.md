# 中断状态 — 2026-08-13

**五条工作线在同一刻因为 API 周额度用尽而终止**（重置：2026-08-16 04:00 香港时间）。不是缺陷，不是它们做错了什么。

这份文件的存在理由只有一个：**这个项目最贵的一次事故，是一整条工作线在上下文压缩后丢失，直到别人报告控制台变脏才被发现。**半成品全部留在工作区里，没有提交。

---

## 恢复前先做这三件

```bash
git log --oneline -20
git status --short
bash tools/run_tests.sh
```

**以仓库为准，不以这份文件为准。**它写于中断当刻，而 git 永远比它新。

**套件当前是红的，原因已知**：`tests/unit/test_time_prompt.gd` 和 `test_threshold_surfacing.gd` 仍钉着旧的 4 秒 / 2.4 秒，而实现已改成所有者要的 8 秒。**这是下面第 1 条自己造成的，它自己收。**

---

## 五条线，各自停在哪里

### 1. 提示打磨（8 秒 · 意境 · 不张扬的 juice）

**最后一步**：正在写 §1.2 的退场规格与 §2.4 的曲线表，然后是测试登记。

**在工作区里**：`src/ui/breath.gd` · `threshold_note.gd` · `threshold_surfacing.gd` · `time_arc.gd` · `time_prompt.gd` · `ui_layer.gd` · `vital_stroke.gd` · `src/definitions/time_prompt_data.gd` · `data/ui/time_prompt.tres` · `tools/generate_time_prompt.gd` · `tools/measure_prompt_traffic.*` · `tools/capture_breath_strip.*` · 三个测试 · **以及 UI 设计文档本身**。

**未完成的判据**：两个红测试要收掉（是需求变了，不是数字要改）；8 秒下两个提示同时在场的概率要测出来；每个面的**退场**是重点，那是「呵」BREATH 里承载诗意的一半。

**所有者已裁的两件**：数字标注成 `Day 1`，**不给分母**，不用「第几天」；弧上必须有「现在」，且要决定它到底是天体轨迹还是时间轴——两个读法现在互相取消。

### 2. 鸟系统（乌鸦为什么不响）

**根因已找到并证明，修法未上。**

`AudioStreamPlayer3D.max_distance` 是从**听者**量起的硬截断；听者是相机，而相机吊在 90 米的臂上（正交投影下对画面无影响）。`60 < 90.71` → 每一声都被引擎静音。**那三声乌鸦自落地起一次都没响过。**同一道算术静音了 `dog.bark`(60) · `whimper`(45) · `whine`(35) · `growl`(14) · 壁炉(24)。环境音床活下来，只因为它没有声明 `max_distance`。

**最后一步**：正准备带着听者测整个混音，好让重新配平出自数字而不是猜测。

**在工作区里**：`src/rendering/camera_rig.gd` · `src/audio/ambience_director.gd` · `data/wildlife/crow.tres` · `pigeon.tres` · **`tests/framework/audio_meter.gd`** · `tools/measure_audibility.*` · `tools/_probe_meter.*`。

> **`tests/framework/audio_meter.gd` 是这批里最值钱的单个产物。**`AudioServer.get_bus_peak_volume_left_db` 在无头 Dummy 驱动下仍然混音，所以这个项目**第一次可以断言「有声音响了」**，而不只是「`play()` 返回了 true」——那正是第五类假通过（实测连续 44 次 `true` 加一片寂静）。**无论如何不要丢掉这个文件。**

**未做完的**：22 dB 之谜（加上听者后环境音床反而低 22 dB，无法解释——**这是上线的门槛**）· 五个声源同时解除静音后的配平 · 惊飞蒙太奇五个绑定条件里坏掉的三个（**第 2 条最严重：子句被回退且文件注释把条件重述得更窄，于是读起来像满足了**）· `montage_director.gd` 不在任何场景里 · 出生点落在 12 米到达守卫之内 · 所有者新提的**鸽子与乌鸦的颜色辨识度**。

### 3. 雪花的翻滚与阻尼

**最后一步**：正在接材质、贴图与 process material。

**判据**：所有者不喜欢「一簇一簇发射」「线段很僵硬」，要「真实世界的飞落加旋转阻尼曲线」和「艺术氛围感」。

**Director 的指令：先修形状，再谈数量。**「加雪量」那条指令已撤回——排 3 已经让近层 +45.1%、远层 +22.2%，如果飞雪本身的形状是问题，加量只会放大它。

**不许撤销**：排 1（锥角 14→3.5，回绕修正后 29.6°→17.2°，是空间结构的前提）· 排 5（风向记忆）。排 4 已作废。

### 4. 探针与死接线

**最后一步**：正在写证明它的测试。

**三件**：`probe_run_lighting.gd` 的 `--seed` 被解析但从不应用（**同种子两次运行会不同，而差异会被读成「效果」**，且要查它已污染过哪些已提交结论）· `visibility_multiplier = 0.25` 零消费者 · 光照审计把深度雾斜坡报成线性，实为 smoothstep（0.0928/0.4841/0.6327 @ t=0.20/0.50/0.60，故 `pale_day` 恒定霾是 0.165 非 0.20）。

### 5. 足迹加主语（W2-M）

**最后一步**：正在先写测试文件，好让门禁对着未修改的消费者先变红。

**判据**：第二个物种必须**零 `.gd` 改动**，且要在测试里放一个临时物种证明它。第四波山谷、第五波熊来之前必须做完。

---

## 不属于任何一条线的两件

- **`addons/gut/`** 出现在工作区里，来源不明（不是上述五条中任何一条派出去做的）。GUT 是一个测试框架，而这个项目有自己的框架和 `tools/run_tests.sh` 那套控制台门禁。**先查清它是谁加的、要做什么，再决定去留。**
- 所有者自己的一批未跟踪文件（`Refs/` · `Unarmed_RM.glb` · 两个 `.url` · `explosive_anim_importer *.py` · `script_templates/` · `tools/__pycache__/`）。**任何提交都要用显式 pathspec，不要把它们扫进去。**

# 阶段 0：冻结与恢复基线审计

日期：2026-08-14

分支：`main`

审计起点：`33263db`
出口测试：`2375 passed, 0 failed`

## 结论

工作区表面上的“283 个变更”来自 `git status --short` 对未跟踪目录的折叠显示。逐文件展开后的真实起始规模是：112 个已跟踪修改、219 个未跟踪文件，共 331 个文件。

这 331 个文件并非 331 个功能改动。盘点结果如下：

| 分类 | 文件数 | 处理 |
|---|---:|---|
| 信标系统 | 18 | 独立提交；数据、运行时、场景、生成器和测试一起验收 |
| 生存路线 | 37 | 独立提交；4 条路线、20 个节点、运行时层、生成器和测试一起验收 |
| 天气 VFX / 雪雾 | 35 | 独立提交；天气定义、数据、profile、场景、shader、运行时和测试一起验收 |
| 其余有效项目工作 | 217 | 作为已集成基线提交；其中动物/UI/新道具后续冻结 |
| 生成物与缓存 | 16 | 不提交；`build/` 13 个，Python 缓存 3 个 |
| 临时探针残留 | 8 | 删除孤立 `.uid`；对应探针脚本已不存在 |
| **合计** | **331** | — |

阶段 0 自身另外增加 `.gitignore`、本审计和重写后的 `PROGRESS.md`，并从版本库移除误跟踪的 Python 缓存。

## 完成且可独立验收

### 信标

范围：`data/beacons/`、`scenes/entities/beacon/`、`src/entities/beacon/`、`src/systems/beacon_network.gd`、`tools/generate_beacons.gd`、`tests/unit/test_beacon_network.gd` 及对应 UID/验收场景。

现状：5 个数据定义、燃料消耗、天气熄灭、运行开始重置和场景生成均有覆盖。共享 `main.tscn` 接线留在综合集成提交中。

### 生存路线

范围：`data/routes/`、`data/route_nodes/`、两类 route definition、route node entity、`survival_route_layer.gd`、生成器和测试。

现状：4 条路线、20 个节点，具备数据引用、生成和场景表现。共享主场景接线留在综合集成提交中。

### 天气 VFX 与雪雾

范围：`data/weather/vfx/`、`data/weather/fog/`、天气事件资源、两类 profile、天气 VFX/雪雾场景和 shader、生成器及测试。

现状：六种天气拥有数据驱动表现，暴雪与雪雾拥有近地雾配置；昼夜、风和事件钩子由测试覆盖。共享主场景接线留在综合集成提交中。

## 已集成但从现在起冻结

这些文件并非失效代码，当前完整套件全部通过；但它们不属于下一阶段主线，因此基线提交后停止扩展：

| 冻结方向 | 盘点规模 | 说明 |
|---|---:|---|
| 鸽子/喂食/脚印/栖息积雪 | 33 | 已有数据、交互、VFX、截图工具和测试；不再增加动物功能 |
| 暂停/退出/无障碍/UI 音效 | 21 | 已有实现和测试；不再继续排版与动效打磨 |
| 新道具/地标/农场装饰 | 29 | 生成脚本、运行时积雪/碰撞和证据保留；不再新增道具 |
| 其余世界、雪、性能、农舍、交互和启动工作 | 135 | 纳入当前可运行基线，后续只修阻塞主线的缺陷 |

上表的前三类是“已实现但冻结”，不是当前要继续补完的功能。真正的半成品是产品闭环：七日流程、威胁、三种结局、客人事件、存档/死亡恢复。它们不会因为局部测试很多而被标记为完成。

## 生成物与临时探针

不进入版本库：

- `build/` 下的火桶截图、Windows 可执行文件与 zip；
- `tools/**/__pycache__/` 和 `*.pyc`；
- `_focused_footprint_tests`、`_probe_track_mask_d3d12`、雪驱逐/上传/分块等已删除脚本遗留的 8 个 UID。

`build_windows.bat`、`tools/export_windows.ps1` 和 `export_presets.cfg` 是可复现导出的源配置，属于有效项目文件，保留并提交。

## 测试证据

命令：

```bash
bash tools/run_tests.sh
```

结果：

```text
2375 passed, 0 failed
```

包装器退出码为 0，未检测到 `SCRIPT ERROR`、`ERROR:`、`WARNING:`、解析错误、泄漏或缺失总结。用户提到的唯一失败在本次基线运行中已不存在；对应农舍室内揭示场景测试已兼容节点以 `position` 或完整 `transform` 表达位置的两种合法写法。

## 阶段 0 后的提交边界

1. `cad0587`：信标数据与运行时；
2. `6242ce3`：生存路线数据与运行时；
3. `a1d4863`：天气 VFX 与雪雾；
4. `674ce82`：当前其余有效主场景、交互、世界、UI、动物、工具和测试；
5. 本收口提交：仓库卫生与基线文档。

之后进入 `PROGRESS.md` 中的阶段 1。阶段 1 结束前，禁止恢复视觉、动物、暂停菜单和新道具的扩展工作。
